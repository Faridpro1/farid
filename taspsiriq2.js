// Asagidaki xətaları fix edin.

function trimWord(word) {
	return trimWord(word);
}

function getWordLength(word) {
	return word.length;
}

function multiply(a, b, c) {
	a * b * c;
	return multiply(a, b, c);
}